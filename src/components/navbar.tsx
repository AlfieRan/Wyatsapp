import React from "react";
import Image from 'next/image';
import {Box, Text, Heading, Center, Flex } from "@chakra-ui/react";

const navBar = () => {
    return (
        <Center h={"10vh"} w={"100vw"} fontSize={"4vw"} bg={"AltBlue"}>
            <Text>Wyatsapp</Text>
        </Center>
    );
};

export default navBar;
